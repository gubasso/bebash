<!-- STUB — DELETE ME once the real test suite lands. -->

# test/ — TEMPORARY STUB

This directory currently holds only `placeholder.bats`, a throwaway test whose
sole job is to give the `test-unit` pre-commit hook a passing target before the
real bats suite exists.

**Cleanup required.** When the real tests arrive:

1. Build out the layout in [docs/reference/testing.md](../docs/reference/testing.md)
   (`test_helper/` submodules, `common-setup.bash`, `fn_*.bats`, `cmd_*.bats`).
2. Delete `test/placeholder.bats`.
3. Delete this `test/README.md`.
