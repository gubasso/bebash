# shellcheck shell=bash
: 'desc: user config'

# Set user-specific bebash options here.
# User functions, libs, rc.d files, and standalone command sources live under
# $BEBASH_DATA_DIR (default: $XDG_DATA_HOME/bebash).
# Expose standalone commands with explicit symlinks under ~/.local/bin.
